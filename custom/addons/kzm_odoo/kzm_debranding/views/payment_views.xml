<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="acquirer_kanban_inherit" model="ir.ui.view">
            <field name="name">payment.acquirer.kanban.inherit</field>
            <field name="model">payment.acquirer</field>
            <field name="inherit_id" ref="payment.acquirer_kanban"/>
            <field name="arch" type="xml">
                <xpath expr="//t[@t-if='!installed and to_buy']" position="replace">
                </xpath>
            </field>
        </record>
    </data>
</odoo>