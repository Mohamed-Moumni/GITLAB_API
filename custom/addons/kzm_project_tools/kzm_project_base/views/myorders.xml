<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <template id="myorders" name="Mes Commandes">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Mes Commandes</t>
            </t>
            <t t-if="orders" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Numéro #</th>
                        <th class="text-end">Date de la commande</th>
                        <th class="text-end">Client</th>
                        <th class="text-end">Vendeur</th>
                        <th class="text-end">Total</th>
                        <th class="text-end">État de la facture</th>
                    </tr>
                </thead>
                <t t-foreach="orders" t-as="order">
                    <tr>
                        <td>
                            <a t-att-href="order.get_portal_url()">
                                <t t-out="order.name"/>
                            </a>
                        </td>
                        <td class="text-end">
                            <span t-field="order.date_order"/>
                        </td>
                        <td class="text-end">
                            <span t-field="order.partner_id.name"/>
                        </td>
                        <td class="text-end">
                            <span t-field="order.user_id.name"/>
                        </td>
                        <td class="text-end">
                            <span t-field="order.amount_total"/>
                        </td>
                        <td class="text-end">
                            <span t-field="order.invoice_status"/>
                        </td>
                    </tr>
                </t>
            </t>
            <p t-else="">There are currently no orders for your account.</p>
        </t>
    </template>
</odoo>